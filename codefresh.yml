version: '1.0'

steps:
  build-prj:
    type: build
    description: codefresh example
    image-name: codefreshexamples/socketio
    dockerfile: Dockerfile
    tag: latest

  notify:
    tutum/curl:latest
    type: build
    description: codefresh example
    tag: latest
